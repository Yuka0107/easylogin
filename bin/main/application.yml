spring:
  thymeleaf:
    cache: false
  jmx:
    enabled: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:8889/boot1?serverTimezone=Asia/Tokyo
    username: root
    password: root

mybatis:
  configuration:
    mapUnderscoreToCamelCase: true
logging:
  level:
    org:
      springframework: WARN
    jp:
      co:
        internous:
          easylogin:
            model:
              mapper: DEBUG